dist: trusty
sudo: required
language: node_js

node_js:
  - "6"
os:
  - linux
install:
  - npm install
script:
  - npm run lint
after_success:
  - npm run test
  - npm run report-coverage
deploy:
  provider: npm
  email: esta@sbb.ch
  api_key: ${NPM_KEY}
  on:
    tags: true
